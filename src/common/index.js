const {
  PORT_ENV,
  NODE_ENV,
  MONGO_CONNECTION_STRING,
  AUTH_MODE,
  JWT_SECRET_KEY,
} = require("./config");

module.exports = {
  PORT_ENV,
  NODE_ENV,
  MONGO_CONNECTION_STRING,
  AUTH_MODE,
  JWT_SECRET_KEY,
};
